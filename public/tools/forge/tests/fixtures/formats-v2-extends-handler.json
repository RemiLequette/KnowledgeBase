{
  "extensions": {
    "md": { "syntaxAdapter": "./mock-adapter.js" }
  },
  "formats": {
    "bad-format": {
      "extends": "sequence",
      "fileNameExtension": "md",
      "handler": "./mock-handler.js",
      "description": "Illegal — extends + handler declared together."
    }
  }
}
